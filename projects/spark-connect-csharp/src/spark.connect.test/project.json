{
  "name": "spark.connect.test",
  "$schema": "../../../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "targets": {
    "lint": {
      "executor": "@nx-dotnet/core:format",
      "configurations": {
        "ci": {
          "check": true
        },
        "dev": {
          "check": true,
          "fix": true
        }
      },
      "defaultConfiguration": "dev"
    },
    "clean": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "projects/spark-connect-csharp/src/spark.connect.test",
        "command": "rm -rf bin obj"
      }
    },
    "test": {
      "executor": "@nx-dotnet/core:test",
      "dependsOn": ["clean"]
    }
  },
  "tags": []
}
